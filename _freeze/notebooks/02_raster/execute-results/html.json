{
  "hash": "de31660daefa745613e0716a85fa8254",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"Raster\"\nsubtitle: \"Same Salzburg AOI, three cloud-native access patterns, plus two timeseries datacubes\"\nauthor: \"Kshitij Raj Sharma\"\ndate: today\nformat:\n  html:\n    toc: true\n    code-fold: false\n    code-tools: true\n    code-line-numbers: true\n    code-overflow: wrap\n    code-copy: true\n    df-print: paged\n    link-external-icon: true\n    link-external-newwindow: true\n    smooth-scroll: true\n    embed-resources: true\nexecute:\n  echo: true\n  warning: false\n  cache: false\n  freeze: auto\njupyter: python3\n---\n\n[![Open in Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kshitijrajsharma/cng-workshop-materials/blob/master/notebooks/02_raster.ipynb)\n[![Download .ipynb](https://img.shields.io/badge/Download-.ipynb-F37626?logo=jupyter&logoColor=white)](https://github.com/kshitijrajsharma/cng-workshop-materials/raw/master/notebooks/02_raster.ipynb)\n[![Repo](https://img.shields.io/badge/Source-GitHub-181717?logo=github&logoColor=white)](https://github.com/kshitijrajsharma/cng-workshop-materials)\n\n::: {#d751a73d .cell execution_count=1}\n``` {.python .cell-code}\nimport subprocess\nimport sys\n\nif \"google.colab\" in sys.modules:\n    subprocess.check_call(\n        [sys.executable, \"-m\", \"pip\", \"install\", \"-q\",\n         \"pystac-client\", \"rioxarray\", \"xarray\", \"zarr>=3\",\n         \"virtughan\", \"matplotlib\", \"pyproj\", \"shapely\"],\n    )\n```\n:::\n\n\n::: {#4c8bc5ea .cell execution_count=2}\n``` {.python .cell-code}\n%matplotlib inline\nimport json\nimport re\nimport time\nimport urllib.request\nfrom pathlib import Path\n\nimport matplotlib.pyplot as plt\nimport numpy as np\nimport pandas as pd\nimport pyproj\nimport rioxarray  # noqa: F401  registers the .rio accessor\nimport xarray as xr\nfrom pystac_client import Client\nfrom shapely.geometry import box\nfrom shapely.ops import transform as shp_transform\n\nREPO_RAW = \"https://raw.githubusercontent.com/kshitijrajsharma/cng-workshop-materials/master\"\nAOI_URL = f\"{REPO_RAW}/data/aoi/salzburg_bbox.json\"\nBBOX = tuple(json.loads(urllib.request.urlopen(AOI_URL).read())[\"bbox\"])\nxmin, ymin, xmax, ymax = BBOX\n\nTARGET_GRID = \"MGRS-33UUP\"\nUTM_CRS = \"EPSG:32633\"\nto_utm = pyproj.Transformer.from_crs(\"EPSG:4326\", UTM_CRS, always_xy=True).transform\nxmin_u, ymin_u, xmax_u, ymax_u = shp_transform(to_utm, box(*BBOX)).bounds\n\nOUT = Path(\"../data/outputs\") if Path.cwd().name == \"notebooks\" else Path(\"data/outputs\")\nOUT.mkdir(parents=True, exist_ok=True)\nprint(f\"AOI bbox (WGS84)   = {BBOX}\")\nprint(f\"AOI bbox (UTM 33N) = ({xmin_u:.0f}, {ymin_u:.0f}, {xmax_u:.0f}, {ymax_u:.0f})\")\nprint(f\"target MGRS tile   = {TARGET_GRID}\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nAOI bbox (WGS84)   = (13.02, 47.78, 13.1, 47.83)\nAOI bbox (UTM 33N) = (351677, 5293597, 357806, 5299304)\ntarget MGRS tile   = MGRS-33UUP\n```\n:::\n:::\n\n\n::: {#358dbe91 .cell execution_count=3}\n``` {.python .cell-code}\ndef plot_indices(ndvi, ndwi, title):\n    fig, axes = plt.subplots(1, 2, figsize=(11, 4.5))\n    ndvi.plot(ax=axes[0], cmap=\"RdYlGn\", vmin=-0.2, vmax=0.8, cbar_kwargs={\"label\": \"NDVI\"})\n    axes[0].set_title(f\"NDVI, {title}\")\n    axes[0].set_aspect(\"equal\")\n    ndwi.plot(ax=axes[1], cmap=\"RdBu\", vmin=-0.5, vmax=0.5, cbar_kwargs={\"label\": \"NDWI\"})\n    axes[1].set_title(f\"NDWI, {title}\")\n    axes[1].set_aspect(\"equal\")\n    fig.tight_layout()\n    plt.show()\n    plt.close(fig)\n```\n:::\n\n\n## 1. Pick a clean Sentinel-2 scene over Salzburg (STAC)\n\nEarth Search by Element84 is a public STAC API in front of Sentinel-2 L2A on AWS Open Data. We filter by bbox, date window, cloud cover, and the MGRS tile that fully covers our AOI (so every scene comes back in the same UTM grid). The same item identifier and date is reused by the COG, Zarr, and VirtuGhan sections so we are comparing access patterns, not different scenes.\n\n::: {#dd93a7fd .cell execution_count=4}\n``` {.python .cell-code}\nES = Client.open(\"https://earth-search.aws.element84.com/v1\")\nsearch = ES.search(\n    collections=[\"sentinel-2-l2a\"],\n    bbox=BBOX,\n    datetime=\"2025-06-01/2025-09-30\",\n    query={\"eo:cloud_cover\": {\"lt\": 10}, \"grid:code\": {\"eq\": TARGET_GRID}},\n    limit=20,\n)\nitems = sorted(search.items(), key=lambda i: i.properties[\"eo:cloud_cover\"])\nitem = items[0]\nDATE = item.datetime.date().isoformat()\nprint(f\"items returned = {len(items)}\")\nprint(f\"picked id      = {item.id}\")\nprint(f\"date           = {DATE}, cloud = {item.properties['eo:cloud_cover']:.2f}%\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nitems returned = 5\npicked id      = S2C_33UUP_20250919_0_L2A\ndate           = 2025-09-19, cloud = 0.00%\n```\n:::\n:::\n\n\n## 2. COG access, fetch only the bands and pixels you need\n\nEach Sentinel-2 band on Earth Search is a **Cloud Optimized GeoTIFF**. The COG layout (tile-aligned, internally indexed) lets `rioxarray.open_rasterio(...).rio.clip_box(...)` issue HTTP range requests for only the bytes covering the AOI. Three band assets, three lazy reads, no full-scene download.\n\n::: {#879c4817 .cell execution_count=5}\n``` {.python .cell-code}\ndef cog_band(stac_item, asset_key):\n    da = rioxarray.open_rasterio(stac_item.assets[asset_key].href, masked=True).squeeze(drop=True)\n    return da.rio.clip_box(*BBOX, crs=\"EPSG:4326\").astype(\"float32\")\n\nt0 = time.perf_counter()\nred_cog   = cog_band(item, \"red\")\nnir_cog   = cog_band(item, \"nir\")\ngreen_cog = cog_band(item, \"green\")\nndvi_cog = (nir_cog - red_cog) / (nir_cog + red_cog)\nndwi_cog = (green_cog - nir_cog) / (green_cog + nir_cog)\nt_cog = time.perf_counter() - t0\nprint(f\"COG path: {t_cog:.2f} s, AOI shape = {red_cog.shape}\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nCOG path: 5.35 s, AOI shape = (572, 614)\n```\n:::\n:::\n\n\n::: {#fa95aa4b .cell execution_count=6}\n``` {.python .cell-code}\nplot_indices(ndvi_cog, ndwi_cog, f\"COG, Earth Search, {DATE}\")\n```\n\n::: {.cell-output .cell-output-display}\n![](02_raster_files/figure-html/cell-7-output-1.png){width=1043 height=410}\n:::\n:::\n\n\n## 3. Zarr access, the EOPF Sample Service\n\nESA's **EOPF Sample Service** republishes Sentinel-2 L2A as **Zarr** stores, indexed by a STAC API at [stac.core.eopf.eodc.eu](https://stac.core.eopf.eodc.eu/). It is a sample, not the full archive, so it does not always hold the exact date Earth Search picked. We re-run STAC on the EOPF catalog over the same window and grid, and take the cleanest scene it does publish. All 10 m bands live in one Zarr group, so a single `xr.open_dataset(..., group=\"/measurements/reflectance/r10m\")` gives us `b02, b03, b04, b08` together. Slicing in array coordinates fetches only the chunks that intersect the AOI.\n\n::: {#40cf21d4 .cell execution_count=7}\n``` {.python .cell-code}\nEOPF = Client.open(\"https://stac.core.eopf.eodc.eu/\")\neopf_search = EOPF.search(\n    collections=\"sentinel-2-l2a\",\n    bbox=BBOX,\n    datetime=\"2025-05-01T00:00:00Z/2025-09-30T23:59:59Z\",\n    query={\"eo:cloud_cover\": {\"lt\": 20}, \"grid:code\": {\"eq\": TARGET_GRID}},\n    limit=20,\n)\neopf_items = sorted(eopf_search.items(), key=lambda i: i.properties.get(\"eo:cloud_cover\", 100))\neopf_item = eopf_items[0]\nEOPF_DATE = eopf_item.datetime.date().isoformat()\nproduct_url = eopf_item.assets[\"product\"].href\nprint(f\"EOPF items in window = {len(eopf_items)}\")\nprint(f\"picked id            = {eopf_item.id}\")\nprint(f\"date                 = {EOPF_DATE}, cloud = {eopf_item.properties['eo:cloud_cover']:.2f}%\")\nprint(f\"Zarr product         = {product_url}\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nEOPF items in window = 2\npicked id            = S2A_MSIL2A_20250921T100731_N0511_R022_T33UUP_20250921T121857\ndate                 = 2025-09-21, cloud = 7.74%\nZarr product         = https://objects.eodc.eu:443/e05ab01a9d56408d82ac32d69a5aae2a:202509-s02msil2a-eu/21/products/cpm_v256/S2A_MSIL2A_20250921T100731_N0511_R022_T33UUP_20250921T121857.zarr\n```\n:::\n:::\n\n\n::: {#4a10eca3 .cell execution_count=8}\n``` {.python .cell-code}\nt0 = time.perf_counter()\nr10m = xr.open_dataset(\n    product_url,\n    engine=\"zarr\",\n    group=\"/measurements/reflectance/r10m\",\n    consolidated=True,\n)\naoi = r10m.sel(x=slice(xmin_u, xmax_u), y=slice(ymax_u, ymin_u)).load()\nndvi_zarr = (aoi.b08 - aoi.b04) / (aoi.b08 + aoi.b04)\nndwi_zarr = (aoi.b03 - aoi.b08) / (aoi.b03 + aoi.b08)\nt_zarr = time.perf_counter() - t0\nprint(f\"Zarr path: {t_zarr:.2f} s, AOI shape = {aoi.b04.shape}\")\nprint(f\"COG path (different scene, for reference): {t_cog:.2f} s\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nZarr path: 4.43 s, AOI shape = (570, 613)\nCOG path (different scene, for reference): 5.35 s\n```\n:::\n:::\n\n\n::: {#1f53ec17 .cell execution_count=9}\n``` {.python .cell-code}\nplot_indices(ndvi_zarr, ndwi_zarr, f\"Zarr, EOPF, {EOPF_DATE}\")\n```\n\n::: {.cell-output .cell-output-display}\n![](02_raster_files/figure-html/cell-10-output-1.png){width=1044 height=415}\n:::\n:::\n\n\n## 4. VirtuGhan, on-the-fly index from cloud-resident COGs\n\n[VirtuGhan](https://github.com/kshitijrajsharma/VirtuGhan) takes a STAC catalog, a bbox, and a band-math formula, then computes the index directly from cloud-resident COGs and returns just the result. No per-band glue, no intermediate downloads. We give it the same AOI and a tight one-week window around our COG date, ask for the median-aggregated composite, and run it twice: once for NDVI, once for NDWI.\n\n::: {#2eb9b909 .cell execution_count=10}\n``` {.python .cell-code}\nfrom virtughan.engine import VirtughanProcessor\n\nVG_START = (pd.Timestamp(DATE) - pd.Timedelta(days=3)).date().isoformat()\nVG_END   = (pd.Timestamp(DATE) + pd.Timedelta(days=3)).date().isoformat()\n\ndef virtughan_composite(band1, band2, label):\n    out_dir = OUT / f\"vg_single_{label}\"\n    out_dir.mkdir(parents=True, exist_ok=True)\n    proc = VirtughanProcessor(\n        bbox=list(BBOX),\n        start_date=VG_START,\n        end_date=VG_END,\n        cloud_cover=30,\n        formula=\"(band1 - band2) / (band1 + band2)\",\n        band1=band1,\n        band2=band2,\n        operation=\"median\",\n        timeseries=False,\n        output_dir=str(out_dir),\n        smart_filter=False,\n    )\n    proc.compute()\n    return out_dir\n\nt0 = time.perf_counter()\nndvi_dir = virtughan_composite(\"nir\",   \"red\", \"ndvi\")\nndwi_dir = virtughan_composite(\"green\", \"nir\", \"ndwi\")\nprint(f\"VirtuGhan composite ({VG_START} to {VG_END}): {time.perf_counter() - t0:.2f} s\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nEngine starting...\nSearching STAC .....\nTotal scenes found: 8\nScenes covering input area: 6\nScenes after removing overlaps: 2\n\rComputing Band Calculation:   0%|          | 0/2 [00:00<?, ?it/s]\rComputing Band Calculation:  50%|█████     | 1/2 [00:02<00:02,  2.41s/it]\rComputing Band Calculation: 100%|██████████| 2/2 [00:05<00:00,  2.72s/it]\rComputing Band Calculation: 100%|██████████| 2/2 [00:05<00:00,  2.67s/it]\nAggregating results...\nSaving aggregated result with colormap...\nEngine starting...\nSearching STAC .....\nTotal scenes found: 8\nScenes covering input area: 6\nScenes after removing overlaps: 2\n\rComputing Band Calculation:   0%|          | 0/2 [00:00<?, ?it/s]\rComputing Band Calculation:  50%|█████     | 1/2 [00:00<00:00,  1.71it/s]\rComputing Band Calculation: 100%|██████████| 2/2 [00:01<00:00,  1.44it/s]\rComputing Band Calculation: 100%|██████████| 2/2 [00:01<00:00,  1.48it/s]\nAggregating results...\nSaving aggregated result with colormap...\nVirtuGhan composite (2025-09-16 to 2025-09-22): 8.58 s\n```\n:::\n:::\n\n\n::: {#59bcdc1c .cell execution_count=11}\n``` {.python .cell-code}\nndvi_vg = rioxarray.open_rasterio(\n    ndvi_dir / \"custom_band_output_aggregate.tif\", masked=True,\n).squeeze(drop=True)\nndwi_vg = rioxarray.open_rasterio(\n    ndwi_dir / \"custom_band_output_aggregate.tif\", masked=True,\n).squeeze(drop=True)\nplot_indices(ndvi_vg, ndwi_vg, f\"VirtuGhan median, {VG_START} to {VG_END}\")\n```\n\n::: {.cell-output .cell-output-display}\n![](02_raster_files/figure-html/cell-12-output-1.png){width=1042 height=417}\n:::\n:::\n\n\n## 5. Datacube, NDVI time series with xarray + COG\n\nFor a seasonal trend we don't want a single scene, we want every clear scene in the window stacked along a `time` axis. Below: search L2A for the Salzburg growing season, build a `(time, y, x)` cube by lazy-opening each scene's COG bands, take the spatial mean per date, plot the trend.\n\n::: {#d21e14f1 .cell execution_count=12}\n``` {.python .cell-code}\nTS_START, TS_END = \"2025-05-01\", \"2025-09-30\"\nts_search = ES.search(\n    collections=[\"sentinel-2-l2a\"],\n    bbox=BBOX,\n    datetime=f\"{TS_START}/{TS_END}\",\n    query={\"eo:cloud_cover\": {\"lt\": 20}, \"grid:code\": {\"eq\": TARGET_GRID}},\n    limit=50,\n)\nts_items = sorted(ts_search.items(), key=lambda i: i.datetime)\nprint(f\"timeseries scenes (cloud < 20%, {TS_START} to {TS_END}) = {len(ts_items)}\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\ntimeseries scenes (cloud < 20%, 2025-05-01 to 2025-09-30) = 8\n```\n:::\n:::\n\n\n::: {#c2fcdef3 .cell execution_count=13}\n``` {.python .cell-code}\ndef ndvi_for_item(it):\n    red = cog_band(it, \"red\")\n    nir = cog_band(it, \"nir\")\n    ndvi = (nir - red) / (nir + red)\n    return ndvi.expand_dims(time=[pd.Timestamp(it.datetime)])\n\nndvi_cube = xr.concat([ndvi_for_item(it) for it in ts_items], dim=\"time\")\nprint(f\"NDVI cube shape = {ndvi_cube.shape} (time, y, x)\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nNDVI cube shape = (8, 572, 614) (time, y, x)\n```\n:::\n:::\n\n\n::: {#3f9511b5 .cell execution_count=14}\n``` {.python .cell-code}\nndvi_trend_xr = ndvi_cube.mean([\"x\", \"y\"], skipna=True)\nfig, ax = plt.subplots(figsize=(10, 4))\nndvi_trend_xr.plot(ax=ax, marker=\"o\", color=\"#2c7a3d\")\nax.set_title(\"Mean NDVI over Salzburg AOI, xarray cube, Sentinel-2 L2A (COG)\")\nax.set_ylabel(\"Mean NDVI\")\nax.set_xlabel(\"\")\nax.grid(True, alpha=0.3)\nplt.show()\nplt.close(fig)\n```\n\n::: {.cell-output .cell-output-display}\n![](02_raster_files/figure-html/cell-15-output-1.png){width=821 height=357}\n:::\n:::\n\n\n## 6. Datacube, NDVI time series with VirtuGhan\n\nSame date window, same AOI, same Sentinel-2 source, but the per-date NDVI is computed *inside VirtuGhan* against the STAC catalog. We just consume the per-date GeoTIFFs it writes and replicate the same trend chart.\n\n::: {#53b843d9 .cell execution_count=15}\n``` {.python .cell-code}\nvg_ts_dir = OUT / \"vg_ts_ndvi\"\nvg_ts_dir.mkdir(parents=True, exist_ok=True)\n\nt0 = time.perf_counter()\nproc = VirtughanProcessor(\n    bbox=list(BBOX),\n    start_date=TS_START,\n    end_date=TS_END,\n    cloud_cover=20,\n    formula=\"(band1 - band2) / (band1 + band2)\",\n    band1=\"nir\",\n    band2=\"red\",\n    operation=\"median\",\n    timeseries=True,\n    output_dir=str(vg_ts_dir),\n    smart_filter=False,\n)\nproc.compute()\nprint(f\"VirtuGhan timeseries: {time.perf_counter() - t0:.2f} s\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nEngine starting...\nSearching STAC .....\nTotal scenes found: 36\nScenes covering input area: 29\nScenes after removing overlaps: 14\n\rComputing Band Calculation:   0%|          | 0/14 [00:00<?, ?it/s]\rComputing Band Calculation:   7%|▋         | 1/14 [00:00<00:02,  5.32it/s]\rComputing Band Calculation:  14%|█▍        | 2/14 [00:00<00:01,  6.12it/s]\rComputing Band Calculation:  21%|██▏       | 3/14 [00:02<00:13,  1.20s/it]\rComputing Band Calculation:  29%|██▊       | 4/14 [00:05<00:16,  1.63s/it]\rComputing Band Calculation:  36%|███▌      | 5/14 [00:07<00:16,  1.84s/it]\rComputing Band Calculation:  43%|████▎     | 6/14 [00:09<00:16,  2.06s/it]\rComputing Band Calculation:  50%|█████     | 7/14 [00:11<00:14,  2.11s/it]\rComputing Band Calculation:  57%|█████▋    | 8/14 [00:14<00:12,  2.15s/it]\rComputing Band Calculation:  64%|██████▍   | 9/14 [00:16<00:11,  2.23s/it]\rComputing Band Calculation:  71%|███████▏  | 10/14 [00:18<00:08,  2.21s/it]\rComputing Band Calculation:  79%|███████▊  | 11/14 [00:20<00:06,  2.19s/it]\rComputing Band Calculation:  86%|████████▌ | 12/14 [00:23<00:04,  2.21s/it]\rComputing Band Calculation:  93%|█████████▎| 13/14 [00:25<00:02,  2.20s/it]\rComputing Band Calculation: 100%|██████████| 14/14 [00:28<00:00,  2.45s/it]\rComputing Band Calculation: 100%|██████████| 14/14 [00:28<00:00,  2.03s/it]\nAggregating results...\nSaving aggregated result with colormap...\nCreating GIF and zipping TIFF files...\nSaved timeseries GIF to ../data/outputs/vg_ts_ndvi/output.gif\nSaved intermediate images ZIP to ../data/outputs/vg_ts_ndvi/tiff_files.zip\nVirtuGhan timeseries: 31.68 s\n```\n:::\n:::\n\n\n::: {#7a5ac688 .cell execution_count=16}\n``` {.python .cell-code}\nrecords = []\nfor tif in sorted(vg_ts_dir.glob(\"*_result.tif\")):\n    m = re.search(r\"(\\d{8})T(\\d{6})\", tif.name)\n    if not m:\n        continue\n    date = pd.to_datetime(m.group(1) + m.group(2), format=\"%Y%m%d%H%M%S\")\n    arr = rioxarray.open_rasterio(tif, masked=True).squeeze(drop=True).values\n    records.append((date, float(np.nanmean(arr))))\n\nndvi_trend_vg = pd.DataFrame(records, columns=[\"date\", \"ndvi_mean\"]).sort_values(\"date\")\nfig, ax = plt.subplots(figsize=(10, 4))\nax.plot(ndvi_trend_vg[\"date\"], ndvi_trend_vg[\"ndvi_mean\"], marker=\"o\", color=\"#0b6e4f\")\nax.set_title(\"Mean NDVI over Salzburg AOI, VirtuGhan, Sentinel-2 L2A\")\nax.set_ylabel(\"Mean NDVI\")\nax.set_xlabel(\"\")\nax.grid(True, alpha=0.3)\nplt.show()\nplt.close(fig)\n```\n\n::: {.cell-output .cell-output-display}\n![](02_raster_files/figure-html/cell-17-output-1.png){width=832 height=357}\n:::\n:::\n\n\n## References\n\n- Earth Search v1: <https://element84.com/geospatial/introducing-earth-search-v1-new-datasets-now-available/>\n- pystac-client: <https://pystac-client.readthedocs.io/>\n- Cloud Optimized GeoTIFF: <https://cogeo.org/>\n- EOPF Sample Service: <https://eopf-toolkit.github.io/eopf-101/>\n- VirtuGhan: <https://github.com/kshitijrajsharma/VirtuGhan>\n- Sentinel-2 L2A bands: <https://sentiwiki.copernicus.eu/web/s2-products>\n\n",
    "supporting": [
      "02_raster_files"
    ],
    "filters": [],
    "includes": {}
  }
}
